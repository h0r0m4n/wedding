<!DOCTYPE html>
<html lang="{{ lang }}">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% if title %}{{ title }} – {{ site.title[locale] }}{% else %}{{ site.title[locale] }}{% endif %}</title>
        <style>
            /* Webfonts ----------------------------------------------------- */

            @font-face {
                font-family: 'Merriweather Sans';
                font-style: normal;
                font-weight: 300;
                src: local(''), url('/static/merriweather-sans-v21-latin-300.woff2') format('woff2'), url('/static/merriweather-sans-v21-latin-300.woff') format('woff');
            }

            @font-face {
                font-family: 'Merriweather Sans';
                font-style: italic;
                font-weight: 300;
                src: local(''), url('/static/merriweather-sans-v21-latin-300italic.woff2') format('woff2'), url('/static/merriweather-sans-v21-latin-300italic.woff') format('woff');
            }

            @font-face {
                font-family: 'Merriweather Sans';
                font-style: normal;
                font-weight: 600;
                src: local(''), url('/static/merriweather-sans-v21-latin-600.woff2') format('woff2'), url('/static/merriweather-sans-v21-latin-600.woff') format('woff');
            }

            @font-face {
                font-family: 'Merriweather Sans';
                font-style: italic;
                font-weight: 600;
                src: local(''), url('/static/merriweather-sans-v21-latin-600italic.woff2') format('woff2'), url('/static/merriweather-sans-v21-latin-600italic.woff') format('woff');
            }

            @font-face {
                font-family: 'Merriweather Sans';
                font-style: normal;
                font-weight: 800;
                src: local(''), url('/static/merriweather-sans-v21-latin-800.woff2') format('woff2'), url('/static/merriweather-sans-v21-latin-800.woff') format('woff');
            }

            @font-face {
                font-family: 'Merriweather Sans';
                font-style: italic;
                font-weight: 800;
                src: local(''), url('/static/merriweather-sans-v21-latin-800italic.woff2') format('woff2'), url('/static/merriweather-sans-v21-latin-800italic.woff') format('woff');
            }

            @font-face {
                font-family: 'Barlow Condensed';
                font-style: normal;
                font-weight: 700;
                src: local(''), url('/static/barlow-condensed-v12-latin-700.woff2') format('woff2'), url('/static/barlow-condensed-v12-latin-700.woff') format('woff');
            }

            @font-face {
                font-family: 'Barlow Condensed';
                font-style: italic;
                font-weight: 700;
                src: local(''), url('/static/barlow-condensed-v12-latin-700italic.woff2') format('woff2'), url('/static/barlow-condensed-v12-latin-700italic.woff') format('woff');
            }

            /* Variables ---------------------------------------------------- */

            :root {
                /* Colours -------------------- */
                --color-background: #fff;
                --color-base: #1b1a18;
                --color-brand: #141f96;
                --color-brand-hover: #2e389f;
                --color-highlight: #d2c75e;

                /* Typography ----------------- */

                /* Paragraph */

                --text-paragraph-family: 'Merriweather Sans', sans;
                --text-paragraph-line-height: 1.5;
                --text-paragraph-weight: 300;
                --text-paragraph-weight-italic: 300;
                --text-paragraph-weight-bold: 600;
                --text-paragraph-weight-bold-italic: 600;

                /* Heading */

                --text-heading-family: 'Barlow Condensed', sans;
                --text-heading-line-height: 1;
                --text-heading-weight: 700;
                --text-heading-weight-italic: 700;

                /* Spacing -------------------- */

                --space-tiny: 0.5rem;
                --space-small: 1rem;
                --space-medium: 2rem;
                --space-large: 4rem;
            }

            /* Base --------------------------------------------------------- */

            html {
                overflow-x: hidden;
                scroll-behavior: smooth;
            }

            body {
                margin: var(--space-small);
                font: var(--text-paragraph-weight) 1em/var(--text-paragraph-line-height) var(--text-paragraph-family);
                background-color: var(--color-background);
                color: var(--color-base);
            }

            @media (min-width: 37.5rem) {
                body {
                    margin: var(--space-medium);
                }
            }

            @media (min-width: 50rem) {
                body {
                    display: grid;
                    grid-template-columns: 1fr 1fr;
                    grid-template-columns: repeat(2, 1fr);
                    gap: var(--space-large);
                    margin: var(--space-large);
                }
            }

            @media (min-width: 50rem) {
                footer {
                    grid-column: 1 / -1;
                }
            }

            h1, h2, h3, h4, h5, h6 {
                font-family: var(--text-heading-family);
                font-weight: var(--text-heading-weight);
                line-height: var(--text-heading-line-height);
                text-transform: uppercase;
            }

            h1 { font-size: 4rem }
            h2 { font-size: 2.5rem }
            h3 { font-size: 2rem }
            h4 { font-size: 1.75rem }
            h5 { font-size: 1.5rem }
            h6 { font-size: 1rem }

            h2 + p::first-letter {
                float: left;
                font-family: var(--text-heading-family);
                font-weight: var(--text-heading-weight);
                text-transform: uppercase;
                font-size: 3rem;
                margin-top: calc(var(--space-small) * -1);
                margin-right: var(--space-tiny);
                color: var(--color-brand);
            }

            p {
                margin: 0;
                orphans: 2;
            }

            p + p { text-indent: 2ch }

            i, em, address { font-weight: var(--text-paragraph-weight-italic) }

            strong, b { font-weight: var(--text-paragraph-weight-bold) }

            a { color: var(--color-brand) }
            a:hover { color: var(--color-brand-hover) }

            hr {
                column-span: all;
                border: 0;
                border-bottom: 1px solid var(--color-highlight);
                clear: both;
                display: block;
                height: 1px;
                margin: var(--space-small) 0;
                padding: 0;
            }

            @media (min-width: 37.5rem) {
                hr { margin: var(--space-medium) 0 }
            }

            /* Helpers ------------------------------------------------------ */

            @media (min-width: 50rem) {
                .columns-2 {
                    column-count: 2;
                    column-gap: var(--space-large);
                    /* column-rule: 1px solid var(--color-highlight); */
                }
            }

            /* Layout ------------------------------------------------------- */
        </style>
    </head>
    <body>
        {{ content | safe }}
    </body>
</html>