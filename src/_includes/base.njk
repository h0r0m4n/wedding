<!DOCTYPE html>
<html lang="{{ locale }}">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% if title %}{{ title }} – {{ site.title[locale] }}{% else %}{{ site.title[locale] }}{% endif %}</title>
    </head>
    <body>
        <header>
            <h1>{% if title %}{{ title }}{% else %}{{ site.title[locale] }}{% endif %}</h1>
        </header>
        <main>
            {{ content | safe }}
        </main>
    </body>
</html>